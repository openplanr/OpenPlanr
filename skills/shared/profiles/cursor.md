# Cursor host profile

Uses structured chat as the preferred guided-question surface. An attached
terminal is a runtime-verified fallback, while headless execution is limited to
exact repository context and explicit safe defaults.
