# Claude Code host profile

Presents bounded guided questions through the host's native composer when that
capability is available. Structured chat and an attached terminal are ordered
fallbacks; headless execution may only consume exact repository context or an
explicit safe default declared by the question.
