# Pipeline capability-aware host profile

Projects composed-v1 skills for the host-neutral `pipeline` target and declares
the runtime-verified interaction surfaces introduced by the 1.1.0 profile. An
attached terminal is used only when the runtime reports it; otherwise headless
execution consumes exact context or safe defaults only. The overlay continues
to narrow the skill to read-only repository access.
