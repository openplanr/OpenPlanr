---
id: "{{id}}"
title: "{{title}}"
storyId: "{{storyId}}"
specId: "{{specId}}"
slug: "{{slug}}"
schemaVersion: "{{schemaVersion}}"
type: "{{type}}"
agent: "{{agent}}"
status: "{{status}}"
created: "{{date}}"
updated: "{{date}}"
---

# {{id}} — {{title}}

> **User Story:** {{storyId}}
> **Spec:** {{specId}}
> **Type:** {{type}}
> **Agent:** `{{agent}}` (matches `planr-pipeline` subagent name; free-form for other tools)
> **Note:** T-NNN ID is **scoped to the parent spec** ({{specId}}), not project-globally unique.

## Objective

{{#if objective}}{{objective}}{{else}}_One paragraph describing what this task must accomplish._{{/if}}

## Files

### Create

{{#if filesCreate.length}}
{{#each filesCreate}}
- `{{this}}` — _purpose_
{{/each}}
{{else}}
- `path/to/new-file.ext` — _purpose_
{{/if}}

### Modify

{{#if filesModify.length}}
{{#each filesModify}}
- `{{this}}` — _what to change_
{{/each}}
{{else}}
- `path/to/existing-file.ext` — _what to change_
{{/if}}

### Preserve (do not touch)

{{#if filesPreserve.length}}
{{#each filesPreserve}}
- `{{this}}` — _why_
{{/each}}
{{else}}
- `path/to/protected-file.ext` — _why_
{{/if}}

## Technical Spec

{{#if technicalSpec}}
{{technicalSpec}}
{{else}}
{{#if (eq type "UI")}}
- **Components to build:** _list with props_
- **Routes/pages to create:** _list_
- **Design tokens to apply:** _from design-spec.md sections (if present)_
- **State to manage:** _local | global | server_
- **API calls to wire up:** _endpoint names from sibling Tech tasks — do not implement endpoint here_
{{else}}
- **Endpoints to create:** _METHOD /path → response shape_
- **Services to implement:** _ServiceName.MethodName(params) → return type_
- **DTOs to define:** _RequestDto / ResponseDto_
- **DB operations:** _SELECT / INSERT / UPDATE / DELETE on which tables_
- **Entities to create or modify:** _EntityName fields_
- **Business logic:** _rules to enforce in code_
{{/if}}
{{/if}}

## Test Requirements

{{#if testRequirements}}
{{testRequirements}}
{{else}}
- **Unit tests:** _what to test, expected behavior_
- **Integration tests:** _if applicable_
- **Edge cases to cover:** _list_
{{/if}}

## Definition of Done

- [ ] All files listed under "Create" exist and compile
- [ ] All files listed under "Modify" are updated correctly
- [ ] All files listed under "Preserve" are unchanged
- [ ] Build command exits 0 (from `input/tech/stack.md` `BuildCommand`)
- [ ] Test command exits 0 (from `input/tech/stack.md` `TestCommand`)
- [ ] No regressions in related features
- [ ] Smoke check passes (if applicable)
