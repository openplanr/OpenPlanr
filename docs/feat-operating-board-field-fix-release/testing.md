> Generated by Doc-Gen Agent on 2026-07-31T18:05:00Z

# Test Evidence Map — Operating Board Field-Fix Release

Source: `qa-report.md` (single round, PASSED). All counts below are from the QA audit's
final, re-run verification — no retries occurred for this spec (no
`T-*-error-report.md` files exist under `.planr/specs/SPEC-002-.../tasks/`).

## Exact final counts

```
openplanr — default config:  111 files passed / 1087 tests passed
openplanr — heavy config:     22 files passed /   95 tests passed
openplanr — build:            npm run build, exit 0, tsc clean

planr-pipeline @ v0.34.0 — full npm test (schema, orchestration, docs, design,
  design-engine, artifact, dashboard, check:operating-assets,
  check:guided-adapters, conformance:operate [incl. v1.3], conformance:design,
  conformance:design-loop, conformance:artifact, conformance:dashboard):
  exit 0, no failures
```

These counts "match the notes-file baseline exactly, no deviation either direction" per
`qa-report.md`'s Summary table — i.e. the delivered suite is exactly what was planned,
with no missing or unexpectedly-added coverage discovered during the audit (contrast
with the previous release, where the round-2 delta was tracked explicitly because two
tasks needed a retry).

## Test evidence by functional requirement

| FR | Story / Task | Proving suite(s) | Result |
|----|---------------|-------------------|--------|
| FR1 — mission dispatch wired end-to-end | US-003 / T-003 (engine) + US-001 / T-001 (pipeline) | `tests/unit/operate-adapter-mission-dispatch.test.ts` (tripwire 1); `tests/orchestration/operating-adapter-handoff.test.mjs` (pipeline, `dispatch.agent`) | 5/5; PASS |
| FR2 — pack budgets fail closed | US-002 / T-002 | `tests/unit/operate-advisor-pack-scale.test.ts` (tripwire 2) | 3/3 |
| FR3 — human review renderer | US-004 / T-004 | `tests/unit/operate-command.test.ts` ("renders the review gate as Markdown by default and raw state only under --json") | PASS |
| FR4 — board-identity binding | US-005 / T-005 | `tests/unit/operate-adapter-lifecycle.test.ts` (board-identity-collision, supersede-not-cancel); `tests/unit/operate-evidence-sources.test.ts` (workspaceDigest-mismatch recollect); `tests/unit/operate-initialization-replay.test.ts` (committed-apply purge) | all PASS |
| FR5 — masked crash eliminated | US-006 / T-006 | `tests/unit/operate-advisors.test.ts:856` ("surfaces a provider bootstrap failure as a typed E_OPERATE_ADVISOR_FAILED with remedy" — tripwire 3) | 1/1 (isolation run) |
| FR6 — real runtime detection | US-006 / T-006 | `tests/integration/operate-guided-init.test.ts` ("detects the host runtime from env markers instead of stamping unknown/none"); `operate-advisors.test.ts`'s T-006 describe block (auto resolution, native-read-only recognition) | 8/8 (describe block) |
| FR7 — continuation is not failure | US-004 / T-004 | `tests/unit/operate-command.test.ts` (first-use consent `ok:true`/`flow:'handoff'`); `tests/integration/operate-guided-init.test.ts` (guided-stage advance); `tests/integration/operate-question-resume.test.ts` (tripwire 4, wiring proof) | PASS; PASS; 5/5 |
| FR8 — executable action contracts | US-004 / T-004 | `tests/integration/operate-guided-init.test.ts` (`confirmArgv` present); `tests/integration/operate-preview-boundaries.test.ts` (`confirmationDigest === null` on `run`) | PASS |
| FR9 — questionnaire integrity and diet | US-001 / T-001 (schema) + US-007 / T-007 (engine) | `tests/unit/operate-question-session.test.ts`; `tests/unit/operate-question-engine.test.ts`; `tests/unit/operate-evidence-sources.test.ts`; `tests/unit/operate-charter-suggestions.test.ts`; `tests/integration/operate-guided-init.test.ts`; `tests/integration/operate-question-resume.test.ts` (tripwire 4) | all PASS; 5/5 |
| FR10 — adapter lease ergonomics | US-008 / T-008 | `tests/unit/operate-adapter-lifecycle.test.ts` (lease-surfaced, refresh-on-record, configurable-duration) | PASS |
| FR11 — doctor diagnoses the real thing | US-005 / T-005 | `tests/unit/operate-doctor-staleness.test.ts` (new) | 4/4 |
| FR12 — coordinated release | US-009 / T-009 | No automated test — content-verified by direct read of `.changeset/operate-board-field-fix-release.md` plus the Pin-Surface Verification table (below); release-order/ledger checks are human/external-action items, not code | n/a |

## The four hard-preserve security/crash suites

Four suites are treated as load-bearing regression gates across every task in this
spec — QA checks `git diff main` is **empty** for all four, on top of confirming they
still pass, in the same run as every new suite:

1. `tests/unit/operate-honeytoken-isolation.test.ts` (3/3) — the pack (v1.2) dispatch
   path's empty-tool honeytoken suite.
2. `tests/unit/operate-mission-honeytoken-isolation.test.ts` (11/11) — the mission
   (v1.3) dispatch path's read-only isolation suite, introduced in the previous release
   and carried forward pristine through this one's mission-dispatch wiring (T-003).
3. `tests/unit/operate-journal-crash.test.ts` (7/7) — the write-ahead journal's
   crash-safety coverage.
4. `tests/integration/operate-storage-migration.test.ts` (6/6, heavy config) — the
   previous release's storage-layout migration crash-injection suite, carried forward
   pristine through this release's board-identity and evidence-cache changes (T-005).

All four are named explicitly in every task's Preserve list (T-003, T-005, T-008) and
confirmed pristine and passing by QA in the Summary table.

## The four QA tripwires

`qa-report.md` names four specific, individually-verified tripwires — each checked not
just for test-title presence but for the exact asserted content:

1. **`tests/unit/operate-adapter-mission-dispatch.test.ts`** (T-003, FR1) — 5/5 PASS.
   Verified assertions: `recordAction.dispatch.agent === 'operating-strategy-finance'`;
   `dispatch.missionPacketPointer`; `dispatch.isolation ===
   'enforced-read-only-bounded'`; `start.protocolVersion === '1.3.0'` on the second
   (`maintenance.ts:855`) call site, distinct from the first; citations threading
   through `gateRecordedProposalCitations`; a mixed-mode cycle (default role mission,
   override role pack); the structured-provider-only adapters failing closed to the
   pack path (`isolation === 'enforced-empty-tools'`, `protocolVersion` stays `1.2.0`).
2. **`tests/unit/operate-advisor-pack-scale.test.ts`** (T-002, FR2) — 3/3 PASS (the
   task's own Create-file spec described roughly two tests; the delivered file has
   three — an extra assertion, not a deficiency). Verified: `FIELD_INCIDENT_BYTES =
   2_736_185` against `TECHNOLOGY_RISK_BUDGET = 393_216` throws
   `code: 'E_OPERATE_EVIDENCE_BUDGET'` before any dispatch/adapter call, at both call
   sites, with no session persisted.
3. **`operate-advisors.test.ts:856`** (T-006, FR5) — exact title match: "surfaces a
   provider bootstrap failure as a typed E_OPERATE_ADVISOR_FAILED with remedy". Verified
   assertions: `code: 'E_OPERATE_ADVISOR_FAILED'`, message names the missing-key remedy
   (`planr config set-key <provider>` and `--offline`), `details` matches
   `{ errorClass: 'AIError:missing_key' }`. 1/1 PASS in isolation.
4. **`tests/integration/operate-question-resume.test.ts`** (T-004 + T-007 combined,
   FR7/FR9) — 5/5 PASS at the final tree (heavy config). This file was the intersection
   point of two tasks' edits landing in the same wave-1 window (see
   `implementation.md`): T-004's `ok:false → ok:true, flow:'handoff'` shape change (2
   occurrences) and T-007's questionnaire diet (`timezone`/`cadence` no longer
   submitted, `known-unknowns` no longer submitted, `product-stage: 'Growth' →
   'growth'`). The diff against `main` shows exactly these two combined field-fixes and
   nothing else.

## Pin-Surface Verification

Every pin surface for `planr-pipeline@0.34.0` was independently re-verified by QA
against the actual files, not trusted from the task's own checklist:

| Surface | Expected | Result |
|---|---|---|
| `package.json` `optionalDependencies.planr-pipeline` | `0.34.0` | PASS |
| `package-lock.json` resolved tarball | `0.34.0` | PASS |
| `.github/workflows/{ci,publish,release}.yml` `ref:` | `v0.34.0` (all three) | PASS |
| `tests/e2e/operate-packed-install.test.ts` | `0.34.0` | PASS |
| `tests/e2e/operate-guided-packed-install.test.ts` | `0.34.0` | PASS |
| `.changeset/operate-board-field-fix-release.md` | pins `0.34.0`, no stale strings | PASS |
| `npm view planr-pipeline@0.34.0 version` | `0.34.0` | PASS |
| Stale `0.33.1` in the working-tree diff | none introduced | PASS (the only repo-wide `0.33.1` hits are the untouched, out-of-scope prior-release doc set and a pre-existing fixture literal, both unrelated to this spec) |

## What "single-round PASS" means for this spec

Unlike the previous release (two round-1 failures, both for missing test evidence on
safety-critical detection/recovery behavior), every one of this spec's nine tasks
passed QA on the first audit: every named Create/Modify/Preserve obligation matched the
actual diff (or, for T-001, the merged sibling-repository state); every Definition-of-
Done line was backed by a named, independently re-run passing test; all four tripwires
were confirmed present with their exact asserted content, not just matching titles; all
four hard-preserve suites were byte-pristine and passing; and both the build and the
full test matrix were green with an exact match to the expected baseline.
