> Generated by Doc-Gen Agent on 2026-07-31T18:05:00Z

# Implementation Notes — Operating Board Field-Fix Release

Source: `.planr/specs/SPEC-002-operating-board-field-fix-release/tasks/T-*.md`,
`qa-report.md`, and `.planr/specs/SPEC-002-operating-board-field-fix-release/.run-manifest.jsonl`.
All nine tasks are owned by `backend-agent`; all are `status: "done"`; all nine passed
QA in a single round — no `T-*-error-report.md` files exist under this spec. File paths
below were verified against the actual tree at `src/services/operate/`,
`src/services/ai-service.ts`, `src/services/runtime-manager-service.ts`, and
`src/cli/commands/operate.ts`.

---

## Wave-1 concurrency: six agents on a live tree

`/ship` dispatched this spec's independent tasks concurrently against the same working
tree. Per the run manifest, six tasks — **T-001, T-002, T-004, T-005, T-006, T-008** —
all started at `14:53:00Z`, the moment their preconditions (none — all six are
dependency-free) were satisfied. Two tasks with a declared `dependsOn: ["T-001"]` —
**T-003** and **T-007** — started at `16:12:00Z`, immediately after T-001 (the sibling
`planr-pipeline` publish) finished at `16:08:00Z`. **T-009**, which depends on T-003,
started at `17:25:00Z`, three minutes after T-003 finished at `17:22:00Z`. This is the
same dependency-gated concurrency model documented for the previous release
(`SPEC-014`'s "file overlap alone is not a reason to add `dependsOn`"): stories share
files advisorially (T-002/T-003/T-006/T-008 all touch `src/services/operate/advisors.ts`
or `maintenance.ts`; T-004/T-006/T-007 all touch `src/services/operate/index.ts`) but
only two genuine output dependencies were declared.

Six agents landing on the same handful of shared files in the same ~90-minute window
(`14:53`–`16:42`) is exactly the shape that produces **mid-wave transient reds**: a test
run captured mid-merge, before a later agent's still-in-flight edit to a shared file
(most visibly `tests/unit/operate-adapter-lifecycle.test.ts`, modified by T-002, T-005,
and T-008 in the same wave, and `tests/integration/operate-guided-init.test.ts`,
modified by T-004, T-006, and T-007 across both waves) lands. Each owning agent resolved
its own transient red against the final merged state of its declared files before
marking its task `done`; QA's full-suite re-run against the final tree (the counts in
[`testing.md`](./testing.md)) is what actually gates the release, not any individual
agent's in-flight run. No task's final DoD evidence depends on a transient intermediate
state.

T-003 — the longest single dispatch window in this spec (`16:12:00Z`–`17:22:00Z`, 70
minutes, versus 6–34 minutes for every other task) — experienced one connection-drop
mid-dispatch and resumed from its last committed progress; the resumed dispatch is what
is reflected in the final `files_modified` list and the QA-verified diff. This is
reported operationally by the orchestrator, not independently reconstructable from the
coarse-grained run manifest (which records only start/end timestamps and final file
lists, not mid-dispatch events) or from any `T-003-error-report.md` (none exists,
consistent with a resumed-and-completed dispatch rather than a failed one). QA's
per-task audit of T-003 (below) found no defect attributable to the resume.

---

## T-001 — Publish planr-pipeline 0.34.0 mission-dispatch contracts

**Repository:** sibling `/Users/asemabdou/Work/planr-pipeline`, merged as PR #92 (squash
`37edf4f`), tagged `v0.34.0`, published to npm — the external-publish precondition every
engine-side task in this repository pins.

**Built:** `agent: \`operating-${roleId}\`` added to `missionDispatch()` in
`lib/operate/adapter-handoff.mjs`, consumed by the v1.3 `recordAction` branch; an
additive, optional `"preselected": { "type": "boolean" }` on
`schemas/v1.2.0/guided-question.schema.json`'s choice items (`additionalProperties:
false` and `required` unchanged); `registry/operating-roles.json` bumped to
protocolVersion 1.3.0 with an explicit `dispatchMode` on all six roles; the modernized
`registry/adapters.json` (protocolVersion/pipelineVersion/adapter versions at 1.3.0/
0.34.0; the native-capable adapter's dispatch capability renamed to `native-read-only`;
the other two adapters' capability renamed/confirmed at `structured-provider`); the
mission branch and the interim exit-4-is-handoff rule documented in
`commands/operate.md` and the generated skill template.

**Key files (sibling repository):**
- `lib/operate/adapter-handoff.mjs`
- `schemas/v1.2.0/guided-question.schema.json`
- `registry/operating-roles.json`
- `registry/adapters.json`
- `commands/operate.md`, `templates/runtime/planr-operate-skill.md.tpl`
- `schemas/v1.3.0/operating-adapter-handoff.schema.json` (undeclared but justified — see below)

**Notable deviations (all disposed as justified in `qa-report.md`):**
- Two non-listed pipeline tests updated
  (`tests/orchestration/guided-interaction-contracts.test.mjs`,
  `tests/schema/ecosystem-contracts.test.mjs`) — confirmed both are semantics-preserving
  updates encoding the renamed registry capability values, not assertion weakening.
- **Undeclared-but-justified:** `schemas/v1.3.0/operating-adapter-handoff.schema.json`
  was also modified to add the `agent` field's schema shape
  (`{"type":"string","pattern":"^operating-[a-z][a-z0-9-]{0,63}$"}`) — necessary because
  the schema's `additionalProperties: false` would otherwise reject the new field
  `adapter-handoff.mjs` now emits. Additive, non-breaking, load-bearing for the task's
  own DoD item 1.
- Stale prose left in `docs/guided-operating-board.md` and `ADR-009` — not corrected in
  this task, carried forward as a non-blocking follow-up (see `drift.md`).

**Tests:** `tests/orchestration/operating-adapter-handoff.test.mjs`
(`dispatch.agent === "operating-strategy-finance"`, both the enforced and
fail-closed-to-structured-provider branches), `tests/schema/operating-schemas.test.mjs`
(`preselected` with/without validates; registry protocolVersion 1.3.0 assertions),
`npm run conformance:operate` ("Operating Board v1.3 conformance passed"),
`tests/orchestration/guided-interaction-contracts.test.mjs`, `npm run
check:guided-adapters` — all green; full sibling `npm test` (schema, orchestration,
design, artifact, dashboard, all conformance gates) passes.

---

## T-002 — Enforce pack input budgets at every construction call site

**Built:** `assertOperatingAdvisorPackWithinBudget()` (new,
`src/services/operate/advisors.ts:584`) measures the canonicalized pack's byte length
against the role's budget, reusing the existing `E_OPERATE_EVIDENCE_BUDGET` code (no new
`OperateErrorCode` minted); `resolveOperatingPackBudget()` (`advisors.ts:563`) resolves
the budget from the pipeline's role registry with a 256 KiB fallback when unpublished;
`createOperatingAdvisorPack` calls both at its return point (`advisors.ts:551-552`); the
false `advisors.ts:481` docstring ("pack mode still enforces") is corrected in place;
`operateAdapterLifecycle`'s prepare branch (`maintenance.ts:1413`) surfaces the same
budget failure instead of silently constructing an oversized session.

**Key files:**
- `src/services/operate/advisors.ts`
- `src/services/operate/maintenance.ts`
- `tests/unit/operate-advisor-pack-scale.test.ts` (new)
- `tests/unit/operate-advisors.test.ts` (extended)
- `tests/unit/operate-adapter-lifecycle.test.ts` (extended)

**Notable deviation (disposed as justified):** the task text left budget-source
ambiguous; the implementation sources it from the protocol role registry (via
`resolveOperatingPackBudget()`), not a field newly added to a readiness type — confirmed
via direct read, no new `OperateErrorCode` minted, `types.ts` untouched by this task.

**Tests:** `tests/unit/operate-advisor-pack-scale.test.ts` (3/3 — the task's Create file
specified building the scale test at the field incident's exact byte count; the
delivered file has one extra assertion beyond the two the task text estimated, noted by
QA as "an extra assertion, not a deficiency"), asserting
`FIELD_INCIDENT_BYTES = 2_736_185` against `TECHNOLOGY_RISK_BUDGET = 393_216` throws
`E_OPERATE_EVIDENCE_BUDGET` at both the `dispatchOperatingAdvisors` and
`operateAdapterLifecycle` prepare call sites, with no provider invocation and no
session persisted. `tests/unit/operate-mission-packet.test.ts` — zero diff, 5/5 pass
unmodified (the mission-mode budget path this task deliberately does not touch).

---

## T-003 — Wire real mission dispatch through the native adapter lifecycle

**Built:** `operateAdapterLifecycle`'s prepare branch resolves each requested role's
dispatch mode via `resolveOperatingDispatchMode` +
`operatingRuntimeEnforcesBoundedReadOnly(cycle.producer.runtime)`; roles resolving
`mission` get a real `buildOperatingMissionPackets` output stored as a new
`roleMissionPackets` field on `PrivateAdvisorSession`, exposed at
`/data/missionPackets/<role>`; both `adapterHandoff()` and the prepare-required start
handoff pass `protocolVersion: '1.3.0'` when `sessionHasMissionRole()` is true; the
`'record'` branch reads from `session.roleMissionPackets` and threads citations into
`gateRecordedProposalCitations`; `protocol.ts`'s `createOperatingAdapterHandoff` input
gained a passthrough-only `protocolVersion?: string`; `advisors.ts`'s `dispatchRole`
branches execution on the resolved mode and captures `RoleDispatchProvenance` at
dispatch time so provenance reads what was actually dispatched, not a re-derivation.

**Key files:**
- `src/services/operate/maintenance.ts`
- `src/services/operate/protocol.ts`
- `src/services/operate/advisors.ts`
- `tests/unit/operate-adapter-mission-dispatch.test.ts` (new)
- `package.json`, `.github/workflows/{ci,publish,release}.yml`,
  `tests/e2e/{operate-packed-install,operate-guided-packed-install}.test.ts` (pin bumps
  to `0.34.0`)

**Notable deviations (disposed as justified):**
- Citations resolved at record time into v1.2-valid committed results via
  `gateRecordedProposalCitations`, because the installed schema forbids raw citations —
  confirmed by reading `createNativeMissionOperatingRoleResult`'s citation-resolver
  injection pattern.
- `declaredRoots` narrowing happens via an evidence pre-filter with the digest-bound
  packet staying authoritative — consistent with observed `narrowMissionRootsToCeiling`
  usage.
- The pipeline v1.3 handoff's per-project per-role pack override pointer cannot fully
  express a `dispatchModeOverrides` entry — recorded as a pipeline-repository follow-up
  (see `drift.md`), not an engine defect.
- **Minor additional finding (non-blocking):** `protocol.ts`'s
  `validateProtocolArtifact` also gained an `options?: { protocolVersion?: string }`
  parameter beyond the literally-named `createOperatingAdapterHandoff` passthrough —
  necessary for validating v1.3 mission responses against the correct schema version.
  Additive, non-breaking.

**Tests:** `tests/unit/operate-adapter-mission-dispatch.test.ts` (5/5 — one of this
spec's four named QA tripwires; see `testing.md`) — proves the mission packet (not a
pack) reaching the record action, both handoff call sites at `protocolVersion: '1.3.0'`,
citations threading through `gateRecordedProposalCitations`, a mixed-mode cycle via
`--dispatch-mode-override`, and the structured-provider-only adapters failing closed to
the pack path. All four hard-preserve suites confirmed pristine (`git diff main` = 0)
and passing in the same run.

---

## T-004 — Render review as text, treat continuation as success, make action contracts executable

**Built:** `lifecycle.ts`'s `readOperatingReview` gains a `human?: boolean` option that
reuses `reports.ts`'s `readOperatingReport().markdown` (falling back to
`renderOperatingBrief` when no governed cycle exists yet — still Markdown, never raw
JSON); `--json` (`human` omitted) keeps returning the exact raw state. `index.ts`'s
`handoffContinuation()`/`success()`/`failure()` return `ok:true, flow:'handoff'` for
`E_OPERATE_INPUT_REQUIRED` and the policy-disclosure form of
`E_OPERATE_AUTHORITY_REQUIRED` — a plain mutation-without-`--yes`
`E_OPERATE_AUTHORITY_REQUIRED` stays `ok:false`/exit-4 via `isProviderConsentHandoff()`'s
narrow discriminator. `publicActionCommands`/`attachStructuredActions` attach
`confirmArgv` on digest-confirmable actions and force `confirmationDigest: null` for
`operate run` (no `--confirm` flag).

**Key files:**
- `src/services/operate/lifecycle.ts`
- `src/services/operate/reports.ts`
- `src/services/operate/index.ts`
- `src/cli/commands/operate.ts`
- `tests/unit/operate-command.test.ts`, `tests/integration/operate-guided-init.test.ts`

**Notable deviations (disposed as justified):**
- `types.ts` gained additive `flow?: 'handoff'` and `confirmArgv?: readonly string[]`
  fields, outside the declared Modify list — confirmed additive-only (tsc necessity), no
  removed or narrowed fields.
- **Four non-listed test files updated**, all reviewed line-by-line and confirmed
  necessary and non-weakening: `tests/integration/operate-charter-assistance.test.ts`
  (`ok:false → ok:true, flow:'handoff'`), `tests/unit/operate-facade.test.ts` (same
  shape change, `exitCode:4` assertion removed), `tests/integration/operate-question-resume.test.ts`
  (2 occurrences — the file this task's edit and T-007's questionnaire diet both
  touched, one of this spec's four named QA tripwires), and
  `tests/integration/operate-preview-boundaries.test.ts` (the FR8 null-confirmationDigest
  assertions).

**Tests:** `tests/unit/operate-command.test.ts` — "renders the review gate as Markdown
by default and raw state only under --json"; "discloses and explicitly confirms
first-use provider consent before retrying a run" (`ok:true, flow:'handoff'`);
`tests/integration/operate-guided-init.test.ts` — "returns Protocol input_required
instead of invalid config when JSON input is missing"; "hands the runner a directly
executable confirmArgv on a digest-confirmable action";
`tests/integration/operate-preview-boundaries.test.ts` —
`requiresConfirmation === false`, `confirmationScope === null`,
`confirmationDigest === null` for `planr operate run` on a structured-provider-routed
runtime.

---

## T-005 — Bind adapter sessions and the evidence cache to board identity; diagnose staleness

**Built:** `PrivateAdvisorSession` gains a `boardIdentity: string` field
(`maintenance.ts`); the finalized-session branch checks binding against
`committedBoardIdentity(store)` and supersedes a session from a superseded generation
instead of throwing `E_OPERATE_STATE_INVALID`; `evidenceBoardIdentity()` (new,
`evidence.ts`) — the event-chain genesis hash — is folded into the incremental cache
key; `readIncrementalEvidence` now compares the loaded record's `workspaceDigest`
against the current workspace, treating a mismatch as no-baseline;
`initializationApplyAction` (`index.ts`) calls the new
`purgeBoardMachineLocalCaches()` (defined in `maintenance.ts`, shared with `operate
cache purge` and doctor's scoped fix) to purge `<localRoot>/advisors/` and
`evidence/incremental/`; `doctor.ts` gained two new diagnostics —
`operate-adapter-sessions` and `operate-incremental-baseline` — each with a scoped `fix`
command (`planr operate cache purge --yes`); `runtime-manager-service.ts`'s lock-drift
branch softens to `warn` only when drift is CLI-component-only
(`upgradeOnlyDrift = drift && !genuineDrift && cliDrift`, verified at line 1359) — digest/
adapter drift or non-CLI component drift (e.g. a stale skill bundle) stays `fail`.

**Key files:**
- `src/services/operate/maintenance.ts`
- `src/services/operate/evidence.ts`
- `src/services/operate/index.ts`
- `src/services/operate/doctor.ts`
- `src/services/runtime-manager-service.ts`
- `tests/unit/operate-doctor-staleness.test.ts` (new)

**Notable deviations (disposed as justified):**
- Board identity is the event-chain genesis hash, not a separate epoch field — confirmed
  via `committedBoardIdentity()`/`evidenceBoardIdentity()`.
- Two dead-session state-machine assertions were kept, with supersede tests added beside
  them, consistent with the DoD's own wording ("the same lines that previously asserted
  the hard-cancel behavior").
- The finalized-branch error code was corrected in-place (task text said
  `E_OPERATE_STATE_INVALID`; the actual thrown code differs) — not independently
  re-derivable from the diff alone, but the supersede test passing with neither code
  thrown confirms the fixed behavior either way.
- Lock-drift softening is scoped to CLI-component drift only — confirmed by the
  dedicated test's two-phase assertion (CLI-only → `warn`; skills-only → `fail`).

**Tests:** `tests/unit/operate-doctor-staleness.test.ts` (4/4, new) — both new
diagnostics with their scoped fix commands, and the CLI-only-vs-skills-only lock-drift
boundary. `tests/unit/operate-adapter-lifecycle.test.ts` — board-identity-collision and
supersede-not-cancel assertions. `tests/unit/operate-evidence-sources.test.ts` —
workspaceDigest-mismatch-triggers-recollect. `tests/unit/operate-initialization-replay.test.ts`
— committed-apply purge. `tests/unit/operate-doctor.test.ts` and
`operate-doctor-v13.test.ts` — zero diff, untouched.

---

## T-006 — Type provider bootstrap failures and detect real runtime identity

**Built:** `isAIConfigured` (`ai-service.ts`) kept synchronous and behaviorally
unchanged; a new `resolveAIProviderReadiness()` performs the real key-resolvability
preflight (handles `ollama` as always key-ready, resolves real provider keys via
`resolveApiKey`, returns an actionable `remedy`). `createConfiguredStructuredAdapter`
(`advisors.ts`) wraps `getAIProvider(config)` in try/catch, converting any thrown error
to `E_OPERATE_ADVISOR_FAILED` with the actionable remedy and a redacted `errorClass`.
`index.ts`'s `failure()` records a redacted error class/name for `E_OPERATE_INTERNAL`; a
new `detectOperatingHostRuntime()` probes environment markers specific to each of the
three supported host environments; `resolvedOperatingRuntime()` resolves a
persisted/absent `auto` preference to the detected host; `usesNativeOperatingAdvisors`
now recognizes `native-read-only` alongside `native-isolated`/`native-bounded`.
`question-registry.ts`'s runtime question becomes detect-don't-ask via new
`effectiveDetectedRuntime()`.

**Key files:**
- `src/services/ai-service.ts`
- `src/services/operate/advisors.ts`
- `src/services/operate/index.ts`
- `src/services/operate/interaction/question-registry.ts`
- `tests/unit/operate-advisors.test.ts`, `tests/integration/operate-guided-init.test.ts`

**Notable deviations (disposed as justified):**
- `isAIConfigured` kept synchronous rather than converted to async — 15 out-of-scope
  callers left untouched; `resolveAIProviderReadiness` is additive, not a signature
  change.
- DoD proofs confined to two sanctioned test files via exported seams
  (`resolveAIProviderReadiness`, `detectOperatingHostRuntime` reachable indirectly).
- The run-preview integration proof is delegated to T-004's file
  (`tests/integration/operate-preview-boundaries.test.ts`, non-listed by either task's
  Files section) — confirmed landed and reviewed.

**Tests:** all run under `operate-advisors.test.ts`'s "T-006 — typed provider bootstrap
and runtime detection (FR5/FR6)" describe block (8/8). This block includes the tripwire
test at line 856 — "surfaces a provider bootstrap failure as a typed
E_OPERATE_ADVISOR_FAILED with remedy" — one of this spec's four named QA tripwires (see
`testing.md`). `tests/integration/operate-guided-init.test.ts` — "detects the host
runtime from env markers instead of stamping unknown/none".

---

## T-007 — Diet the onboarding questionnaire and carry preselected choices schema-to-UI

**Built:** `question-registry.ts` — `timezone` removed entirely;
`cadence`/`componentRoots` no longer force-required; `planningEngine` reworded and
detects `pipeline-po` when planr-pipeline is installed; `sources` gates
`github`/`linear` and marks genuinely detected sources `preselected: true`; charter
`businessModel`/`idealCustomer` gain `"Not yet specified"` deferral defaults;
`knownUnknowns` becomes non-required; `product-stage` becomes a real select;
`guardrails` seeded with standing boundaries as suggestions. `question-engine.ts`
narrows the returned `questions` set to required-missing plus explicitly-flagged only.
`index.ts`'s JSON-init context gains `gitUserName` (git-user probe) and honestly-probed
`availableSources`. `answer-service.ts` — shared sources-availability probing helper
used by both `index.ts` and `terminal-renderer.ts`. `cli/commands/operate.ts` —
`--answers-file <path>` alias sharing `readBoundedStdin`'s bounded parser.
`terminal-renderer.ts` renders `preselected` choices pre-checked.

**Key files:**
- `src/services/operate/interaction/question-registry.ts`
- `src/services/operate/interaction/question-engine.ts`
- `src/services/operate/interaction/answer-service.ts`
- `src/services/operate/index.ts`
- `src/services/operate/interaction/terminal-renderer.ts`
- `src/cli/commands/operate.ts`

**Notable deviations (disposed as justified):**
- `types.ts` untouched — `preselected` validates against the T-001-published 0.34.0
  schema without any local type change.
- **Four non-listed test fixture alignments** — confirmed across
  `operate-authority-boundaries.test.ts` (`productStage: 'Growth' → 'growth'`),
  `operate-guided-automation.test.ts` (honest-sources fixture diet), and
  `operate-question-resume.test.ts` (timezone/cadence/known-unknowns removed from
  submitted answers, `product-stage: 'Growth' → 'growth'` — this file's mid-wave
  transient red, discussed above, was the combination of this task's diet and T-004's
  `ok:false → ok:true` shape change landing on the same file) — no assertion weakening
  observed in any.
- "Explicitly flagged" is implemented as `valueSemantics: 'suggestion'`, consistent with
  the `guardrails`/`sources` `preselected`/suggestion wiring.

**Tests:** `tests/unit/operate-question-session.test.ts` — no timezone question, no
longer batches cadence/component-roots, suggests `pipeline-po`.
`tests/unit/operate-question-engine.test.ts` — reaches `preview-ready` with
cadence/component-roots unanswered. `tests/unit/operate-evidence-sources.test.ts` — no
github/linear until configurable in-flow; renders preselected pre-checked, never
fabricates a select. `tests/integration/operate-guided-init.test.ts` — suggests decision
owner from git user on the JSON path; `--answers-file` bounded-parity alias.
`tests/unit/operate-charter-suggestions.test.ts` — "Not yet specified" deferral
defaults, known-unknowns optional, product-stage select, guardrails seeded suggestions.
`tests/integration/operate-question-resume.test.ts` (5/5) — one of this spec's four
named QA tripwires (see `testing.md`).

---

## T-008 — Surface, refresh, and make configurable the adapter session lease

**Built:** the hardcoded `15 * 60 * 1_000` in `maintenance.ts` replaced with
`readOperatingAdapterLeaseDurationMs()`; prepare/record responses surface
`leaseStatus` (`expiresAt` + remaining); the `record` branch refreshes `expiresAt` on a
successful record. `config.ts` gained a new `adapterLeaseDurationMs` field on the
machine-local preferences record (not `OperatingConfig` — see deviation), with
`normalizeOperatingAdapterLeaseDurationMs()` bounds-validating `[60_000, 3_600_000]` ms
and failing closed with `E_OPERATE_CONFIG_INVALID` on an out-of-range/non-integer value
(no silent clamping). `cli/commands/operate.ts` surfaces lease/remaining in human output.

**Key files:**
- `src/services/operate/maintenance.ts`
- `src/services/operate/config.ts`
- `src/cli/commands/operate.ts`
- `tests/unit/operate-adapter-lifecycle.test.ts` (extended)

**Notable deviation (disposed as justified):** lease duration lives in machine-local
`preferences.json` (`OperatingPreferencesRecord.adapterLeaseDurationMs`), not
`OperatingConfig` as the task text named — the same precedent as `dispatchModeOverrides`
from the previous release, since the v1.2 on-disk config schema is frozen
(`additionalProperties: false`). No CLI setter flag was added — confirmed correctly out
of scope.

**Tests:** `tests/unit/operate-adapter-lifecycle.test.ts` — "surfaces the lease expiry
and remaining time in prepare output and handoff (default 15 minutes)"; "refreshes the
lease on each successful record and still enforces expiry after the window lapses";
"honors a machine-local configured lease duration on prepare". All four hard-preserve
suites pass unmodified.

---

## T-009 — Prepare the in-repo release artifact and external release checklist

**Built:** `.changeset/operate-board-field-fix-release.md` (new, untracked) —
declares an `openplanr` minor bump (`1.17.0` → `1.18.0`) pinning
`planr-pipeline@0.34.0`, references `SPEC-002`, describes every FR1–FR11 fix with the
exact tripwire test name inlined for the mission-dispatch claim. No source files
modified — `package.json` and the three workflow files were already finalized by T-003.

**Key files:**
- `.changeset/operate-board-field-fix-release.md`

**Notable decision:** per this repository's boundary rule ("never writes into sibling
repositories"), only the verifiable DoD boxes are checked; every sibling-repository
action (`@openplanr/skills@1.20.0`, and the marketplace's `ecosystem.json`/
protocol-block/adapter-capability/plugin-pin/`SPEC-005` two-step
stage-finalize-ledger/`releaseOperation` update) is recorded as an explicit, unchecked
"external action" checklist item. The one pre-checked external-action item
(`planr-pipeline` tag + CI green) was independently re-verified by QA: `npm view
planr-pipeline@0.34.0 version` → `0.34.0`; all three workflow refs `v0.34.0`.

**Tests:** n/a — no source-code DoD to test; content verified by direct read of the
Changesets entry and by the Pin-Surface Verification table in `qa-report.md` (see
`testing.md`).
