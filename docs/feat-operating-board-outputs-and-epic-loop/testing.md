> Generated by Doc-Gen Agent on 2026-08-01T05:00:00Z

# Test Evidence Map — Operating Board Truthful Outputs, Evidence Integrity, and the Epic Loop

Source: `qa-report.md` (round 1 PASS with two adjudicated deviations; round 2 re-audit
of the T-006 reachability completion, final verdict PASSED). All counts below are from
QA's own live re-runs, not trusted from any task's self-report.

## Exact final counts (round 2, gating)

```
Build (npm run build):        exit 0, no output (silent tsc success + template copy)
Default suite (npx vitest run):
  Test Files  111 passed (111)
       Tests  1102 passed (1102)
Heavy suite (npx vitest run --config vitest.heavy.config.ts):
  Test Files   24 passed (24)
       Tests  104 passed (104)
Lint (npm run lint -> biome check src/ tests/):
  0 errors, 1 warning (pre-existing, hard-preserve file, see below)
Sibling repo (planr-pipeline): v0.35.0 tagged, merged (PR #93 / 115270d), published
  (npm view planr-pipeline@0.35.0 version -> 0.35.0); its own npm test exits 0
```

The default suite (111 files / 1102 tests) is unchanged between round 1 and round 2 —
the new reachability test lives under `tests/integration/`, which the default config
excludes (heavy-only). The heavy suite grew from **23 files / 102 tests (round 1)** to
**24 files / 104 tests (round 2)** — exactly the expected +1 file / +2 tests from
`tests/integration/operate-epic-route-reachability.test.ts`. Both counts match the
expected baseline exactly, with no unexplained delta in either direction.

## Test evidence by functional requirement

| FR | Story / Task | Proving suite(s) | Result |
|----|---------------|-------------------|--------|
| FR1 — files are the source of truth | US-002 / T-002 | `tests/unit/operate-projection-persistence.test.ts` (byte-match `.toBe()` + I/C/E + evidence-ref assertions); `tests/unit/operate-projection.test.ts` (not-evaluated fallback, Status ignores enabledRoles, report-hint string); `tests/integration/operate-preview-boundaries.test.ts` (live `readOperatingReport()` `.toContain()` cross-check) | PASS |
| FR2 — evidence never lost silently | US-003 / T-003 | `tests/unit/operate-evidence-diagnostics.test.ts` (maxFiles truncation detail); `tests/unit/operate-evidence-sources.test.ts` (dot-prefixed drop count in warnings); `tests/unit/operate-advisors.test.ts` (sensitivity narrowing scoped to offending item); `tests/integration/operate-evidence-recovery.test.ts` (starved-role gating) | PASS (one DoD line — evidence-index.json per-source detail — partially satisfied; see `drift.md`) |
| FR3 — prioritized, fair collection | US-003 / T-003 | `tests/integration/operate-evidence-monorepo-fairness.test.ts` (tripwire 1) | PASS |
| FR4 — mission budgets sized for real repositories | US-004 / T-004 (engine) + US-001 / T-001 (pipeline) | `tests/unit/operate-advisors.test.ts` ("no longer caps a large registry budget at the arbitrary 9 KiB ceiling"); `tests/unit/operate-mission-packet.test.ts` (tripwire 8); `tests/orchestration/operating-mission-packet.test.mjs` (pipeline, tripwire-equivalent) | PASS |
| FR5 — re-init preserves preferences | US-005 / T-005 | `tests/unit/operate-initialization-replay.test.ts` (tripwire 6, field repro); `tests/integration/operate-guided-init.test.ts` (preview names changed keys) | PASS |
| FR6 — backlog promotion, projections retirement | US-002 / T-002 | `tests/integration/operate-preview-boundaries.test.ts`, `tests/integration/operate-legacy-import.test.ts` (tripwire 5) | PASS |
| FR7 — epic loop, rendering | US-006 / T-006 | `tests/integration/operate-decision-brief-render.test.ts` ("renders one planr epic create suggestion naming both accepted findings") | PASS |
| FR8 — epic loop, route-kind closure | US-006 / T-006 (round 1) + T-006 reachability completion (round 2) | `tests/integration/operate-route-lanes.test.ts` (tripwire 7 — schema round-trip, apply/rollback, R1); `tests/integration/operate-epic-route-reachability.test.ts` (round 2 — reachability, idempotence, R1 re-confirmed) | PASS |
| FR9 — coordinated release | US-001 / T-001 + US-007 / T-007 | No automated test for the in-repo release artifact — content-verified by direct read of `.changeset/operating-board-outputs-and-epic-loop.md` and `docs/implementation/OPERATE-SPEC-006.md`; the pipeline half is proven by T-001's own suites above; pin-surface checks (below) are code-adjacent, not narrative | PASS |

## The five hard-preserve suites

Five suites are treated as load-bearing regression gates across every task in this
spec — QA confirms `git diff main` is **empty** for all five, on top of confirming they
still pass, in both round-1 and round-2 runs:

1. `tests/unit/operate-honeytoken-isolation.test.ts` — the pack (v1.2) dispatch path's
   empty-tool honeytoken suite.
2. `tests/unit/operate-mission-honeytoken-isolation.test.ts` — the mission (v1.3)
   dispatch path's read-only isolation suite.
3. `tests/unit/operate-journal-crash.test.ts` — the write-ahead journal's crash-safety
   coverage (load-bearing for T-006's epic-route journal write and T-006 round 2's new
   `commitProposedRoute` call).
4. `tests/integration/operate-storage-migration.test.ts` — the storage-layout migration
   crash-injection suite (load-bearing for T-002's projections-retirement/state.json
   relocation).
5. `tests/unit/operate-adapter-mission-dispatch.test.ts` — the mission end-to-end proof
   carried forward from SPEC-002. Carries the one pre-existing lint warning (see below).

All five are byte-pristine against `main` (`git diff main -- <file>` = 0 bytes for each)
in both QA rounds, and pass as part of both the default and heavy runs.

## The eight round-1 tripwires

`qa-report.md` names eight specific, individually-verified round-1 tripwires — each
checked not just for test-title presence but for the exact asserted content:

1. **`operate-evidence-monorepo-fairness.test.ts` — capped walk + determinism.** PASS.
   Three tests: (a) samples `apps/`, `infra/`, `packages/` under a cap the combined
   tree exceeds — all three product directories represented, `.planr/` never crowds
   them out; (b) determinism — two independent collections select byte-identical item
   order and fingerprint; (c) starved-role gating.
2. **Starved-role gating with a governed gap, other-source roles unaffected.** PASS —
   proven twice, in `operate-evidence-monorepo-fairness.test.ts` and independently in
   `operate-evidence-recovery.test.ts`; both confirm OTHER ready roles still dispatch,
   not a whole-batch throw.
3. **`report.md` byte-equals `readOperatingReport().markdown`; evaluated board carries
   I/C/E + evidence refs.** PASS — confirmed at the implementation level (no
   transformation before persistence) and at three test layers (exact `.toBe()` unit,
   `.toContain()` live-CLI integration, explicit `I3 C3 E4`/evidence-ref assertions).
4. **Board hint exactly `planr operate report <cycleId> --lens <role>`, no `--cycle`;
   Status from advisor-result presence.** PASS — the literal string fix plus an
   explicit proof that an enabled-but-unevaluated role still renders `not_evaluated`.
5. **`projections/` gone; `backlog.md` top-level; `.state/state.json` present;
   existing-tree migration handled.** PASS — the `PROJECTION_ROOT` constant is removed
   entirely; the two owning layout tests assert the retired paths never appear and
   enumerate the exact new top-level file list.
6. **T-005 field repro: re-init without flags preserves all three preferences.** PASS —
   test literally titled "...(field repro)"; byte-for-byte `.toBe()` preservation.
7. **T-006: create-epic previews → applies a real epic → byte-exact rollback; R1
   asserted; existing route kinds unchanged.** PASS — `operate-route-lanes.test.ts`'s
   dedicated `describe('committed Operating Board create-epic route (FR8 / US-006)')`
   block proves every clause, including negative event-log assertions for R1;
   `create-quick-task` branches confirmed byte-unmodified.
8. **T-004: monorepo-scale index truncates with a record + reported drop; small index
   untouched; both packed-e2e green at 0.35.0.** PASS — three new
   `operate-mission-packet.test.ts` tests cover truncation-with-record, per-role
   truncation surfacing, and the untouched-healthy-repo case; both packed-install e2e
   files green (23/23 heavy files in round 1).

## Round-2 adversarial checks (T-006 reachability re-audit)

Round 2 was not a re-run of round 1's evidence — it independently re-derived the
reachability claim from source and probed it adversarially rather than trusting the
plan amendment's own account:

- **Import-list check:** `tests/integration/operate-epic-route-reachability.test.ts`'s
  imports were read directly — `createOperatingRoutePlan` is neither imported nor
  referenced anywhere in the file except the file's own explanatory comment
  (`grep -n "createOperatingRoutePlan"` confirmed this), proving the new test drives
  election purely through `governOperatingFinding`, not a library-level bypass.
- **CLI-surface confirmation, read not inferred:** `operate.ts:845` mapping
  `findings accept` to `findings.accept` → `governOperatingFinding`, and
  `operate.ts:869-924` mapping `routes apply`/`routes rollback` to the existing,
  route-kind-agnostic `applyOrRollbackRoute` handler, were both read directly to
  confirm the reachability test's calls are exactly what the CLI verbs invoke.
- **Adversarial empirical probe for the drift-on-growth case:** QA built a throwaway
  test (`tests/integration/operate-tmp-epic-drift-check.test.ts`) that grew an
  accepted-finding group between election and apply, ran it, confirmed
  `error.code === 'E_OPERATE_ROUTE_DRIFT'` was actually thrown (not just theorized from
  reading the digest-check code path), and then **deleted the file**, confirming zero
  trace via `git status --porcelain`. This is the strongest form of verification in
  either round — not reading code and inferring behavior, but exercising it.
- **Non-interference check:** the round-1 file `operate-route-lanes.test.ts` was
  re-run in isolation (`npx vitest run --config vitest.heavy.config.ts
  tests/integration/operate-route-lanes.test.ts`) with round 2's auto-election now
  active in the background (both related findings accept-elect a second, unrelated
  epic route during this same test's setup) — 3/3 still passed unmodified, confirming
  the new auto-election path does not interfere with T-006's original, hand-constructed
  route test.
- **Manifest cross-check:** `.run-manifest.jsonl` entry #13 (round-2 `ship.task:T-006`)
  was read directly and confirmed to list only `routes.ts`, `lifecycle.ts` as modified
  and only the one new test file as written — independently corroborating the "T-006's
  original contract is untouched" claim rather than accepting it from the task file.

## What "PASSED (round 2)" means for this spec

Round 1 passed all seven tasks' literal Definition-of-Done lines with named test
evidence on the first audit, but adjudicated two non-blocking deviations rather than
treating them as retries: T-003's evidence-index.json per-source detail (schema-blocked
at the wire level in both v1.2 and v1.3) and T-006's create-epic route reachability
(mechanism built and tested, but not yet wired to any operator-facing trigger). Round 2
re-audited only the scoped completion dispatched against the second item, closing it
with a genuinely operator-facing path, confirmed idempotent and fail-safe on growth by
direct empirical probe, without reopening or re-deriving the other six tasks' evidence.
The evidence-index.json deferral remains open, tracked as a named follow-up requiring a
future pipeline schema change — see [`drift.md`](./drift.md).
