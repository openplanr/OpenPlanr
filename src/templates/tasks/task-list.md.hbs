---
id: "{{id}}"
title: "{{title}}"
{{#if storyId}}storyId: "{{storyId}}"{{/if}}
{{#if featureId}}featureId: "{{featureId}}"{{/if}}
created: "{{date}}"
updated: "{{date}}"
status: "pending"
---

# {{id}}: {{title}}

{{#if storyId}}**User Story:** [{{storyId}}](../stories/{{storyFilename}}.md){{/if}}
{{#if featureId}}**Feature:** [{{featureId}}](../features/{{featureFilename}}.md){{/if}}

{{#if artifactSources}}
## Artifact Sources
{{#each artifactSources}}
- **{{this.type}}:** `{{this.path}}`
{{/each}}
{{/if}}

## Tasks

{{#each tasks}}
- [ ] **{{this.id}}** {{this.title}}
{{#each this.subtasks}}
  - [ ] {{this.id}} {{this.title}}
{{/each}}
{{/each}}

{{#if acceptanceCriteriaMapping}}
## Acceptance Criteria Mapping
{{#each acceptanceCriteriaMapping}}
- [ ] {{this.criterion}} ({{this.sourceStoryId}}) → Tasks {{join this.taskIds ", "}}
{{/each}}
{{/if}}

{{#if relevantFiles}}
## Relevant Files
{{#each relevantFiles}}
- `{{this.path}}` — {{this.reason}}
{{/each}}
{{/if}}

## Notes
_Mark tasks complete by checking the boxes above. Use your coding agent (Claude Code, Cursor, Codex) with the generated rules for context-aware implementation._
