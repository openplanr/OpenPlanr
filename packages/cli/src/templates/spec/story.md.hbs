---
id: "{{id}}"
title: "{{title}}"
specId: "{{specId}}"
slug: "{{slug}}"
schemaVersion: "{{schemaVersion}}"
status: "{{status}}"
created: "{{date}}"
updated: "{{date}}"
acceptanceCriteria:{{#unless acceptanceCriteria.length}} []{{/unless}}
{{#each acceptanceCriteria}}
  - id: "{{id}}"
    statement: {{{json statement}}}
{{/each}}
---

# {{id}} — {{title}}

> **Spec:** {{specId}}
> **Note:** This User Story ID is project-global; `specId` records its parent.

## User Story

**As** {{roleAction}},
**so that** {{benefit}}.

## Scope

{{#if scope}}{{scope}}{{else}}_Describe the business boundary of this US. What is IN scope. What is explicitly OUT of scope for this US._{{/if}}

## Acceptance Criteria

{{#if acceptanceCriteria.length}}
{{#each acceptanceCriteria}}
- [ ] **{{id}}** — {{statement}}
{{/each}}
{{else}}
- [ ] Given ..., when ..., then ...
- [ ] All related tests pass.
{{/if}}

## Task Breakdown

| Task | File | Agent | Description |
|------|------|-------|-------------|
{{#if tasks.length}}
{{#each tasks}}
| {{id}} | `../tasks/{{filename}}` | `{{agent}}` | {{title}} |
{{/each}}
{{else}}
| _None_ | _None_ | _None_ | _No implementation tasks have been created._ |
{{/if}}

## Dependencies

- **Depends on:** _none_
- **Blocks:** _none_
- **DB tables involved:** _none_

## Notes

_Edge cases, special handling, or hints for the agent executing the tasks._
