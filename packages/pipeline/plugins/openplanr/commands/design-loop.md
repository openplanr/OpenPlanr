---
description: Compatibility alias for planr-design-loop. The canonical skill owns this workflow.
argument-hint: "<target> [options]"
---

# /planr-pipeline:design-loop

This frozen command is a generated compatibility alias for
`skills/planr-design-loop/SKILL.md`.

Load and follow that canonical skill with the user's arguments unchanged. This
alias preserves the canonical workflow without adding runtime-specific logic.
