---
description: Compatibility alias for planr-ship. The canonical skill owns this workflow.
argument-hint: <slug> [--task T-NNN] [--release-candidate]
---

# /planr-pipeline:ship

This package command is a compatibility alias for
`skills/planr-ship/SKILL.md`.

Load and follow that canonical skill with the user's arguments unchanged. This
alias preserves the canonical workflow without adding runtime-specific logic.

`--release-candidate` selects the separate release-candidate runtime.
