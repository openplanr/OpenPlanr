---
description: Compatibility alias for planr-plan. The canonical skill owns this workflow.
argument-hint: <slug> [brief...] [--dry-run]
---

# /planr-pipeline:plan

This package command is a compatibility alias for
`skills/planr-plan/SKILL.md`.

Load and follow that canonical skill with the user's arguments unchanged. This
alias preserves the canonical workflow without adding runtime-specific logic.
