# planr-pipeline
# No provider credentials are required. Semantic work runs in the active agent host.
# Deterministic rendering and local review services use command-specific options.
# Keep project secrets outside the repository; document new options with placeholders.
