# Pipeline host profile

Projects composed-v1 skills for the host-neutral `pipeline` target: agent
roots, the installed package root, the active project stack directory, and the
`planr-` workflow prefix. The overlay narrows a skill to read-only repository
access and declares no host-only tool authority beyond the composed ceiling.
