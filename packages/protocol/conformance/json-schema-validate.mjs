// Compatibility bridge for byte-preserved Protocol schema modules. The
// implementation stays browser-safe and dependency-free in the Protocol facade.
export { validateJson } from '../src/json-schema.mjs';
