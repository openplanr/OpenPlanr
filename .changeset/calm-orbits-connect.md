---
"openplanr": patch
---

Use the branded OpenPlanr company API by default so hosted customers can sign in and publish without copying an infrastructure URL. Keep `--api-url` as an optional development and self-hosted override.
