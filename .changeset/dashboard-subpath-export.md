---
'planr-pipeline': minor
---

`startDashboard` is available from `planr-pipeline/dashboard`, which loads the dashboard server without the package root. The root `startDashboard` export is deprecated; it still works and still loads the dashboard server when the root is imported.
