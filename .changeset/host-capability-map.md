---
'openplanr': minor
---

Generate an `## OpenPlanr capabilities` section into `CLAUDE.md` and `AGENTS.md` listing every skill with its triggers (and the delegated agents for Claude Code), and add the `planr-openplanr` router skill that picks the best OpenPlanr skill for a request.
