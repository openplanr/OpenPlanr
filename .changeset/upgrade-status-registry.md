---
'openplanr': patch
---

Reconcile `planr upgrade status` against the npm registry's published CLI and the pipeline it bundles instead of the retired marketplace manifest, and report a leftover legacy host plugin as the incompatibility.
