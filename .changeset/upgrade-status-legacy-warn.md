---
'openplanr': patch
---

Keep plugins outside the expected marketplace ids out of the `planr upgrade status` verdict; they stay listed for `planr doctor`'s warning instead of turning a current installation `incompatible`.
