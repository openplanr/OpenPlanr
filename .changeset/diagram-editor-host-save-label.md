---
"@openplanr/artifact": patch
"planr-pipeline": patch
---

Let hosts name the save state with `host.saveLabel(state)`, for example "Saved · revision 8", and start a new diagram from the process template with `createDiagramEditorDraft({ template: 'process' })`.
