---
"@openplanr/artifact": patch
"planr-pipeline": patch
---

Show a read-only view when the editor session can read but not write. Undo, Redo, Arrange, Save and the Shapes tab are not offered, Mermaid copies open on export, and hosts can name the view with `labels.readOnly`, for example "Revision 8 · Read only".
