---
"openplanr": minor
---

Add headless document and zoomable canvas presentations to `planr artifact`,
including `--presentation auto|document|canvas`, resolved JSON output, and the
planr-pipeline 0.27.1 compatibility update.
